# TEST-DESIGN — набор, спроектированный с нуля от текущего кода

Составлено так, будто тестов нет вообще. Для каждой защищаемой поверхности —
минимальный набор НАМЕРЕНИЙ (не файлов), каждое привязано к категории риска:
`инвариант` · `контракт` · `ловушка` · `связность` · `опасное` · `регрессия` · `API`.

Колонка «сейчас»: **есть** / **нет** / **частично**.

---

## A. Публичный контракт компонента

Единственная поверхность, которую видит хост. Ни одного теста сегодня.

| # | Что защищаем | Категория | Сейчас |
|---|---|---|---|
| A1 | Полоса рендерит `visibleSlidesNr` слайдов; запрошенное больше колоды коэрцится к длине колоды | контракт | **нет** |
| A2 | `handle.next()/prev()` двигают целевую страницу: цикл заворачивает, finite упирается | API | **нет** |
| A3 | `onCarouselStatusChange` приходит с корректным снимком и НИ РАЗУ не приходит дважды одинаковым | API | частично (только равенство снимков) |
| A4 | Слот рендерится ровно когда флаг И ребёнок И `canSlide` | контракт | **нет** |
| A5 | `canSlide === false` гасит навигацию целиком: ни клик, ни handle, ни автоплей не диспатчат | контракт | **нет** |
| A6 | `aria-current="step"` стоит на актуальной полосе; вне полосы слайд `inert` | доступность | **нет** |
| A7 | Дефолты применяются к `undefined` и НЕ применяются к явно переданному значению | API | **нет** |
| A8 | `className` мержится с собственными классами, а не заменяет их | API | частично (`mergeStyleMaps` отдельно) |
| A9 | Zod-схема: валидная колода проходит, пустой `content` отвергается, ReactElement принимается | API | **нет** |

## B. Машина состояний

| # | Что защищаем | Категория | Сейчас |
|---|---|---|---|
| B1 | MOVE ±1: цикл заворачивает, finite клампится и не-опит на границе | контракт | есть |
| B2 | GO_TO едет в направлении шкалы точек, НИКОГДА не цикличным сокращением | инвариант | есть |
| B3 | Дальний GO_TO = ограниченный preflight + телепорт + подлёт; ближний едет | ловушка | есть |
| B4 | Повторный клик ретаргетит на визуальную страницу +2 и не убегает дальше при любой длине серии | регрессия | есть |
| B5 | Релиз драга: коммит → шаг, без намерения → снап, уже на цели → idle | контракт | есть |
| B6 | MOTION_SETTLED отличает свежий settle от устаревшего по позиции | ловушка | есть |
| B7 | Реконсиляция: hard reset по `dataKey`/`isFinite`, иначе пропорциональный ремап; идемпотентна; `image` не влияет на идентичность; из `dragging` есть выход | инвариант | есть |
| B8 | Структурные инварианты состояния (контракт dev-диагностики) | инвариант | есть (избыточно) |
| B9 | Обвязка: `dispatch` стабилен по идентичности; диспатч в том же коммите видит НОВЫЙ конфиг | опасное | **нет** |

## C. Семантика движения

| # | Что защищаем | Категория | Сейчас |
|---|---|---|---|
| C1 | Телепорт только когда есть полностью пропускаемая промежуточная страница; тюнинг-агностично | инвариант | есть |
| C2 | Потолок по flight-envelope: непрерывная поездка не медленнее полёта | инвариант | есть |
| C3 | Continuity launch: старт с видимой скорости, пол длительности не тормозит старт | регрессия | есть |
| C4 | Канал планов: монотонный `planId`, дедуп idle и одинаковых follow | связность | есть |
| C5 | Рантер: один ре-план на одно изменение состояния; жест стартует из скоастованной точки; preflight публикует план на ВСЮ команду; unmount отменяет и компоновщик, и контроллер | опасное | **нет** |

## D. Кривые и профили (движок motion)

| # | Что защищаем | Категория | Сейчас |
|---|---|---|---|
| D1 | Профиль покрывает расстояние за длительность при любом делении долей; зоны упорядочены | инвариант | есть |
| D2 | Стопы монотонны, концы точны, плотность выводится из формы кривой | инвариант | есть |
| D3 | Сэмплер: точные концы, монотонность, знак скорости, вырожденный случай | граничные | есть |
| D4 | Контроллер: handoff против snapshot; пассивный сегмент садится без rAF; `wake`; мягкий destroy | опасное | есть |
| D5 | Пиннинг: `null` при throw, play-pending при отказе `startTime` | деградация | есть |
| D6 | `useMotionPaint`: свежее замыкание без переподписки, отписка на unmount | утечки | есть |
| D7 | Композитная поездка: композит/фолбэк/отмена | контракт полки | есть |
| D8 | Переносимость пакета | связность | есть |
| D9 | `motionNow`: фолбэк на `Date.now()` без `performance` | деградация | **нет** (текущий тест проверяет платформу) |

## E. Движок жестов

| # | Что защищаем | Категория | Сейчас |
|---|---|---|---|
| E1 | Поверхность против хрома: `surfaceRef`, `data-drag-ignore` | контракт | есть |
| E2 | Окно поимки: пережавший владеет, быстрый подъём = тап, вертикаль отдаётся браузеру | touch | есть |
| E3 | Привязка значения: якорь на активации, ничего на тапе | API | есть |
| E4 | Коммит: флик против дистанции, каждый читает СВОЮ величину для направления | регрессия | есть |
| E5 | Кривая сопротивления и pause-decay (закон против ride-crawl) | регрессия | есть (+3 арифм.) |
| E6 | Сращивание кинетики релиза: какая скорость в какой примитив | связность | есть |
| E7 | Переносимость ×2 (gesture, kinetic) | связность | есть |

## F. Адаптер жеста в карусели

| # | Что защищаем | Категория | Сейчас |
|---|---|---|---|
| F1 | Коуст через коммит-щель: клампы, направления, перекрёстная цель | граничные | есть |
| F2 | Перевод «долей слота» в px движка, точка калибровки | инвариант | есть |
| F3 | `START_DRAG` ВСЕГДА приходит раньше `END_DRAG` | опасное | **нет** |
| F4 | Выключение `isSwipeOn` посреди драга завершает его, а не подвешивает | опасное | **нет** |
| F5 | Таймер отложенного старта гаснет на unmount | утечки | **нет** |

## G. Домен (чистые функции)

| # | Что защищаем | Категория | Сейчас |
|---|---|---|---|
| G1 | `mod`/`normalizePageIndex` на отрицательных — основа цикличного обхода | off-by-one | есть |
| G2 | Трансформ origin-относителен; полоса инвариантна к сдвигу origin | инвариант | есть |
| G3 | `measureSlotSize`: `(width+gap)/visible`, нулевой счётчик даёт 0 а не NaN, непарсибельный gap = 0 | ловушка | **нет** |
| G4 | Флаги видимости при дробном холде, для любого N | регрессия | есть |
| G5 | Правило URL картинки — общее для рендерера и удержания | связность | есть |
| G6 | Разрешение релиза драга | контракт | есть |
| G7 | Окно рендера: покрывает сегмент, буферизовано, finite клампится, цикл нет | off-by-one | **нет** |
| G8 | `alignedVirtualIndex` (полоса цикла), `pageContaining` (floor) против `nearestPageIndex` (round), `reconciledPageIndex` | ловушка | **нет** (косвенно) |
| G9 | `padDeckToFullPage`: ключи уникальны, длина кратна странице, целая колода возвращается как есть | связность | **нет** |
| G10 | `buildCarouselLayout`: `canSlide`, `virtualLength`, `pageCount`, `dataKey` | инвариант | **нет** (косвенно) |

## H. Модель рендера

| # | Что защищаем | Категория | Сейчас |
|---|---|---|---|
| H1 | Окно расширяется в движении и не размонтирует слайд в полёте; сжимается на settle | ловушка | **нет** |
| H2 | `layoutOrigin` ре-центрируется только на дрейфе в целую полосу | инвариант | **нет** |
| H3 | Кэш `VirtualSlide` отдаёт ТОТ ЖЕ объект, пока у слайда ничего не изменилось | ловушка | **нет** |
| H4 | Клоны за пределами колоды получают отличный `slideKey` | связность | **нет** |

## I. Слайды и изображения

| # | Что защищаем | Категория | Сейчас |
|---|---|---|---|
| I1 | Стор: SSOT статуса, backoff/кап/дедуп, prune, мягкий dispose | таймеры | есть |
| I2 | Гейт полосы: две волны, защёлка на первом исходе | опасное | есть |
| I3 | Вуаль: поднимается на смену сигнатуры, снимается при пропаже bitmap и на teardown | регрессия | есть |
| I4 | `SlideItem`: `<img>` НЕ смонтирован при закрытом гейте; ошибка даёт заглушку; `inert` вне полосы; кликабельность требует флага И обработчика И загрузки | контракт | **нет** |
| I5 | Удержание чистит по данным и никогда не выкидывает рендерящийся URL | связность | **нет** |

## J. Геометрия

| # | Что защищаем | Категория | Сейчас |
|---|---|---|---|
| J1 | Источник слота стабилен; уведомление доходит до подписчика | опасное | есть |
| J2 | Пер-кадровая запись НЕ проходит сквозь живую анимацию, `geometry`-запись проходит; отмена пиннит ДО `cancel()` и будит цикл | ловушка | **нет** |
| J3 | Трек применяет то же правило пропуска кадров, что точки и виджет | связность | **нет** — заблокировано [B-1](./TEST-BUGS.md) |

## K. Видимая позиция

| # | Что защищаем | Категория | Сейчас |
|---|---|---|---|
| K1 | `runningFrameIndex` растёт только на `running` и обнуляется на покоящихся | связность | **нет** |
| K2 | `sampleNow()` и `getSnapshot()` дают РАЗНЫЕ ответы на живом сегменте | ловушка | **нет** |

## L. Автоплей

| # | Что защищаем | Категория | Сейчас |
|---|---|---|---|
| L1 | Ровно один живой таймер в любой момент | таймеры | **нет** |
| L2 | Любой из трёх гейтов останавливает цикл; снятие перевзводит | контракт | **нет** |
| L3 | Hover-пауза дебаунсится; `ignoreHover` отключает её целиком | контракт | **нет** |
| L4 | Отложенный тик перевзводится, а не выстреливает в занятый вьюпорт | опасное | **нет** |
| L5 | На unmount не остаётся ни одного таймера | утечки | **нет** |

## M. Навигация, политика рендера, отчёт хосту

| # | Что защищаем | Категория | Сейчас |
|---|---|---|---|
| M1 | `enabled === false` → ни один хендлер не диспатчит | контракт | **нет** |
| M2 | Таблица истинности слотов; Diagnostic не рендерится в проде НИКОГДА | контракт | **нет** |
| M3 | Хост получает каждый отличающийся снимок и ни одного повторного | API | **нет** |

## N. Фокус и доступность

| # | Что защищаем | Категория | Сейчас |
|---|---|---|---|
| N1 | Фокус из ставшего `inert` слайда уезжает в активную полосу; вне карусели не трогается; без цели уходит на контейнер | доступность | **нет** |
| N2 | Восстановление срабатывает на переход в idle и смену страницы, но не на каждый кадр | доступность | **нет** |

## O. Презентация

| # | Что защищаем | Категория | Сейчас |
|---|---|---|---|
| O1 | Контракт JS→CSS: набор публикуемых переменных, отсутствующий класс = `""` | связность | есть |
| O2 | Стили полос идентичны между двумя перестройками за поездку | ловушка | есть |

## P. Модули пагинации

| # | Что защищаем | Категория | Сейчас |
|---|---|---|---|
| P1 | Вид точки от смещения; цикличный перенос зажигает только дальнюю | регрессия | есть (+4 пересказа) |
| P2 | Смена режима не переякоривает ленту; владение берётся и отдаётся целиком | регрессия | есть |
| P3 | Точка: `<button>` против `<div>` по флагу | контракт | есть |
| P4 | Посадка шага виджета + ограничение цепочки | регрессия | есть |
| P5 | Арифметическая связь досягаемости и пула элементов | инвариант | есть |
| P6 | Симметрия передачи краевых точек | регрессия | есть |
| P7 | Запекание временной кривой в кейфреймы | инвариант | есть |
| P8 | Флейвор follow переключается без переподписки | опасное | есть |

## Q. Структурные стражи

| # | Что защищаем | Категория | Сейчас |
|---|---|---|---|
| Q1 | Границы коробки: `client/` ⇏ `data-gen/`, `data-gen/` самодостаточен | связность | есть |
| Q2 | `GeneratedSlide` присваивается в `Slide` | связность | есть |
| Q3 | Контракт измерения: ни padding, ни border на треке и вьюпорте | ловушка | есть |
| Q4 | Оси вьюпорта: канонические строки из чисел, конфиги генератора им следуют | связность | есть |
| Q5 | `--slide-height` читают и слайд, и сайзер | ловушка | есть |
| Q6 | Имена CSS-переменных совпадают у JS и SCSS | связность | есть (+2 grep'а) |
| Q7 | `index.html` не разошёлся с константами темы | связность | есть |
| Q8 | Единственность стора; дремлющая копия хелпера не импортируется | связность | есть |

## R. Полка: clientState, theme, viewportObservation

| # | Что защищаем | Категория | Сейчас |
|---|---|---|---|
| R1 | Стор медиазапросов: живое чтение до подписки, пере-подписка после дремоты | регрессия | есть |
| R2 | Резолв брейкпоинта по числам, независимо от порядка и имён | ловушка | есть |
| R3 | Канонические медиастроки из осей | связность | есть |
| R4 | Первый кадр `isTouch` / `dataSaver` | регрессия | есть |
| R5 | Тема: auto/explicit, ОС, storage, вкладки, хром браузера | контракт | есть |
| R6 | `useViewportBusy` НИКОГДА не ре-рендерит потребителя | контракт | есть |
| R7 | `useMedia`: `signature` меняется ровно при смене вердикта; объект стабилен при стабильной сигнатуре | опасное | **нет** |
| R8 | `useViewportVisibility`: пересечение × видимость вкладки, отписка | опасное | **нет** |

## S. Генератор данных

| # | Что защищаем | Категория | Сейчас |
|---|---|---|---|
| S1 | Соглашения одного слайда | API | есть |
| S2 | Идемпотентный merge: `id` и ручной `alt` переживают регенерацию | контракт | есть |

---

## Сводка диффа (исходная)

**Покрыто и остаётся: 45 намерений.**

**Отсутствует полностью — 26 намерений:**
A1–A2, A4–A7, A9, B9, C5, D9, F3–F5, G3, G7–G10, H1–H4, I4–I5, J2, K1–K2, L1–L5,
M1–M3, N1–N2, R7–R8.

**Избыточно — 20 кейсов** (см. TEST-AUDIT § «Что именно урезаем»).

**Заблокировано решением по B-1 — 1 намерение:** J3.

Дыра распределена не случайно: **отсутствует ровно то, что не трогали
рефакторами** — композиционный корень, автоплей, фокус, политика рендера,
навигация, модель рендера, чистая геометрия домена.

---

## Что закрыто

Тестов было 523, стало **641**. Ниже — намерения, закрытые исполнением.

| Партия | Намерения | Файлы | Коммит |
|---|---|---|---|
| раскладка | — | 41 переезд + 6 путей | `d679eb3` |
| урезание | −20 кейсов | 9 файлов | `f02b608` |
| домен | G7–G10, G9 | `renderWindow`, `layout`, `deckPadding` | `f7b6654` |
| корень | A1–A2, A4–A8, M2–M3 | `carouselContract` | `4bfa5d1` |
| таймеры + фокус | L1–L5, N1 | `useAutoplay`, `manageFocusShift` | `83a8171` |
| слайд + жест | I4, F3–F5 | `SlideItem`, `useCarouselGesture` | `3c0eac0` |
| модель рендера | H1–H4 | `useSlideRenderModel` | `13b0dcc` |

**Осталось незакрытым — 9 намерений**, все уровня B/C по исходному плану:

| # | Что | Почему отложено |
|---|---|---|
| A9 | zod-схемы | S, чистая проверка контракта |
| B9 | обвязка `useCarouselState` | S |
| C5 | `useMotionRunner` | L — самый крупный из оставшихся |
| D9 | фолбэк `motionNow` | **закрыт** в `f02b608` |
| G3 | `measureSlotSize` | S |
| I5 | удержание изображений | S |
| J2 | компоновщик в `useTrackBinding` | M |
| J3 | правило пропуска кадров у трека | заблокировано решением по [B-1](./TEST-BUGS.md) |
| K1–K2 | `useVisualPosition` | S |
| M1 | гейт `enabled` в навигации | **закрыт** косвенно в `carouselContract` |
| R7–R8 | `useMedia`, `useViewportVisibility` | M |
