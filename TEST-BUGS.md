# Найденные дыры в реализации

Всплыли при критическом пересмотре тестов. **Обе исправлены** по согласованию,
каждая заперта тестом, который проверенно краснеет на до-фиксном коде.

| id | Что | Статус |
|---|---|---|
| B-1 | правило пропуска кадров по разным условиям | исправлено, тест `trackBinding.test.tsx` |
| B-2 | `IntersectionObserver` без защиты | исправлено, тест `useViewportVisibility.test.tsx` |
| B-3 | диагностика ругается на пустую колоду | исправлено, тест `layoutChecks.test.ts` |

---

## B-1 · Общее правило пропуска кадров гейтится по РАЗНЫМ условиям у трёх потребителей

**Серьёзность: medium. Уверенность: высокая (прочитано, не воспроизведено на устройстве).**

`docs/architecture/visual-position.md:46` заявляет: `isDroppedFallbackFrame` —
это ОДНО правило пропуска кадров, общее для каждого пер-кадрового потребителя,
«так что три потребителя не могут рассинхронизироваться».

Фактически потребителей три, а условий два:

| Потребитель | Где | Условие пропуска |
|---|---|---|
| трек | `geometry/useTrackBinding.ts:237` | `!isWaapiSupported()` |
| точки | `modules/Pagination/basic/usePaginationFade.ts:293` | `plan.isFallback` |
| виджет | `modules/Pagination/widget/usePaginationWidgetBinding.ts:511` | `plan.isFallback` |

Это не одно и то же условие. `useMotionRunner.ts:197` ставит `isFallback: true`
всякий раз, когда `startCompositorMotion` вернул `false`, а он возвращает `false`
не только из-за отсутствия WAAPI (`useTrackBinding.ts:126-135`):

- `slot === null` — вьюпорт неизмерим (нулевая ширина, `display: none` у предка);
- `!Number.isFinite(from)` или `to`;
- `stops.length < 2`;
- `startPinnedAnimation` вернул `null`, потому что `element.animate()` **бросил** —
  WAAPI есть, но движок отказал (`shared/engines/motion/compositor/pinnedAnimation.ts:19-23`).

**Сценарий.** Браузер с WAAPI, карусель в свёрнутой вкладке или аккордеоне
(нулевая ширина вьюпорта) → слот `null` → компоновщик не берёт поездку →
план `isFallback: true`. Точки и виджет начинают ронять каждый 4-й кадр,
трек красит все. Ровно та рассинхронизация, ради предотвращения которой правило
и существует.

**Исправлено.** Трек теперь подписан на канал планов и держит собственный
`isFallbackFollowRef` — ровно как точки и виджет: `plan.kind === "follow"`
берёт `plan.isFallback`, любой другой план сбрасывает флаг в `false`. Канал
планов создаётся в композиционном корне выше по порядку хуков, чтобы трек мог
на него подписаться. `isWaapiSupported` из `useTrackBinding` ушёл.
Одно правило, три потребителя, один сигнал.

**Почему это не поймали тесты.** Правило проверено у ДВУХ потребителей из трёх
(`usePaginationFade.test.tsx`, `usePaginationWidgetBinding.test.tsx` — оба
ассертят против `isDroppedFallbackFrame` напрямую). У трека такого теста нет.
Тесты здесь не избыточны, а НЕДОстаточны — см. TEST-PLAN, пункт 17.

---

## B-2 · `useViewportVisibility` конструирует `IntersectionObserver` без защиты

**Серьёзность: medium. Уверенность: высокая (воспроизведено — компонент падает).**

`shared/viewportObservation/useViewportVisibility.ts:27` делает
`new IntersectionObserver(...)` безусловно. Если API нет — layout-эффект
бросает, и падает не хук, а **всё дерево целиком**: карусель не монтируется.

Это внутренняя несогласованность полки. Соседний код в том же проекте
защищается:

| Место | API | Защита |
|---|---|---|
| `geometry/useSlotSizeSource.ts:110` | ResizeObserver | `typeof ResizeObserver !== "undefined"` |
| `slides/useOrientationSwapVeil.ts:52` | `img.decode` | `typeof element.decode === "function"` |
| `motion/compositor/pinnedAnimation.ts:16` | WAAPI | `isWaapiSupported()` |
| `viewportObservation/useViewportVisibility.ts:27` | IntersectionObserver | **нет** |

**Как обнаружено.** Первым же интеграционным тестом: компонент не монтируется
под jsdom вообще. То есть любой хост, у которого есть тесты на своих страницах,
упрётся в это сразу — и не в деградацию, а в исключение.

В браузерах IntersectionObserver есть везде с 2019 года, так что на проде это
не горит. Но полка заявлена переносимой, а «переносимая» и «падает без
опционального браузерного API» — несовместимы.

**Исправлено.** Тот же guard, что у соседей. Без API элемент считается
находящимся на экране, подписка не создаётся, а слушатель `visibilitychange`
остаётся — он в IntersectionObserver не нуждается. Деградация в «всегда виден»
стоит потребителю лишней работы за экраном; деградация в «никогда не виден»
выключила бы его навсегда и молча, а бросок уносил бы всё дерево хоста.

**Обход в тестах остался** (`client/tests/browserEnv.ts` стаблит API — это
внешняя граница), но теперь он ускоряет тесты, а не маскирует падение: без
стаба компонент монтируется и просто считает себя видимым.

---

## B-3 · Диагностика кричит на пустую колоду

**Серьёзность: low (dev-only). Уверенность: высокая (воспроизведено тестом).**

`modules/Diagnostic/checks/layoutChecks.ts` при `rawLength === 0` выдавал
«visibleSlidesNr (3) не должен превышать длину колоды (0)». Но пустая колода —
это не ошибка конфигурации, а хост, у которого ещё не приехали данные. Хост,
рендерящий `<Carousel slidesData={[]}>` на время загрузки, получал это
предупреждение на каждый рендер.

Диагностика — пожарная сигнализация, и у сигнализации ровно два способа
отказать: молчать при пожаре и орать без пожара. Второй здесь хуже: шум, на
который нельзя отреагировать, приучает читателя игнорировать канал, и вместе с
шумом уходят настоящие предупреждения.

**Показательно, что соседняя проверка в том же файле уже была защищена**
(`!layout.canSlide && layout.rawLength > 0`) — автор эту мысль имел, но
применил к одной проверке из трёх.

**Исправлено.** `collectLayoutWarnings` возвращает пустой список при пустой
колоде — единый ранний выход вместо guard'а на каждой проверке.
