# 02-todo — отложенное

Что решено сделать, но ещё не сделано. Не «идеи на будущее»: сюда попадает
только то, у чего есть согласованное решение и место в коде. Закрытый пункт
удаляется отсюда и, если он что-то меняет в устройстве проекта, переезжает в
[`01-facts.md`](./01-facts.md) или [`00-map.md`](./00-map.md).

## 1. Померить React Compiler — когда работа по проекту закончится

Решение по компилятору принято: не включаем, ручная мемоизация остаётся
(`01-facts.md`, H3). Но **после** того, как основная работа по проекту будет
закончена, его хочется поставить и сравнить — не чтобы включить, а чтобы знать
цифры.

Что мерить, до и после:

- размеры бандла против базовой линии (`01-facts.md`, раздел A);
- время сборки (сейчас ~730 мс без Babel);
- поведение карусели на стенде глазами: старт и посадка поездки, перетаскивание,
  автоплей, поворот экрана;
- полный прогон тестов.

Что можно будет убрать, если цифры окажутся в пользу компилятора: два ручных
кэша идентичности (`slideCache` в `useSlideRenderModel`, `laneCacheRef` в
`useCarouselPresentation`) и часть `useMemo`/`useCallback`.

Готовность к включению держится постоянно — это требование к каждой правке,
см. `CLAUDE.md`, раздел про React Compiler.

## 2. Reduced motion, когда хост не передал окружение

Сейчас `userEnvironment` — необязательный проп, и `reducedMotion` при его
отсутствии читается как `false`. Значит хост, который просто поставил
`<Carousel />` и ничего не прокинул, даёт полную анимацию человеку, который
на уровне ОС попросил её отключить. В dev об этом говорит диагностика
(`collectEnvironmentWarnings`), в проде — никто.

**Почему это не чинится одной строкой.** Компонент обязан жить в обеих средах
сразу:

- в простой — хост воткнул карусель и не строил никакого слоя окружения;
- в сложной — у хоста уже есть свой источник среды (в этом репозитории это
  `useMedia` в `app/App.tsx`), и второй, внутренний, читатель `matchMedia`
  означал бы два источника истины об одном и том же.

Отсюда развилка, и она архитектурная, а не косметическая:

| | Что делаем | Чем платим |
| --- | --- | --- |
| 1 | Оставить как есть | В простой среде обещание доступности не выполняется, и молча |
| 2 | Сделать `userEnvironment` обязательным | Забыть нельзя — не соберётся. Но простая среда перестаёт быть простой: минимальная интеграция требует от хоста своего слоя |
| 3 | Внутренний фолбэк, когда пропа нет | Работает из коробки, отраслевое умолчание. Но появляется второй читатель среды и ветка, которой нет в SSR |
| 4 | Фолбэк + явное отключение (`userEnvironment={null}` как «я управляю сам») | Обе среды закрыты честно, ценой третьего состояния пропа |

Разбирать отдельным заходом: решение меняет публичный контракт (ADR-002,
caller-owned inputs) и потому требует своего ADR, а не правки на месте.


## 3. Свести проект к одному языку — после рефакторов

Сейчас язык разделён по расположению: внутри `src/**` английский (6342 строки
прозы), вне — русский (4772 строки: база знаний, правила, вывод инструмента).
Граница однозначна и держится, но два языка в одном репозитории — это то, о чём
приходится помнить.

**Решение принято: сводим к одному языку, но не сейчас.** Перевод делается
последним заходом, когда основные рефакторы закончены и инструменты сверки базы
перестанут быть нужны. Причина в порядке работ, а не в цене: перевод затрагивает
всё разом, и делать его посреди рефакторов означает переводить то, что ещё будет
переписано.

Что придётся сделать вместе с переводом, чтобы он не сломал проверки:

- пометки решений и `CONSTRAINT` в коде — сканеры уже двуязычны, но записи в
  `07-invariants.md` и `10-idioms.md` **цитируют** английские фразы из
  комментариев: цитаты станут битыми, и это поймает проверка якорей;
- `08-tests.md` пересказывает 115 тестовых файлов — при смене 1272 заголовков
  `describe`/`it` записи разойдутся с реальностью;
- `CONFIG.rulesHeading` и разбор «N файлов» в `graph.mjs` читают русские
  заголовки базы.

До того дня действует правило: **инструмент не зависит от языка.** Всё, что он
ищет в прозе — пометки решений, `ОГРАНИЧЕНИЕ`/`CONSTRAINT`, записанные дыры в
тестах, — узнаётся на обоих языках. Сканер, знающий одну сторону, в день
перевода замолчит и пройдёт зелёным, то есть соврёт ровно там, где его читают
как гарантию.

## 4. Мутационное тестирование домена и состояния — ЗАКРЫТО

Инструмент заведён (`stryker.config.json`, `npm run test:mutation`), прогон
сделан, выжившие разобраны поимённо. **79.44 % → 95.98 %**: убито 741, выжило
30, без покрытия 1. Разбор всех 30 с причинами — `08-tests.md`, § E; там же
сказано, зачем он нужен дальше: **выживших стало больше 31 — разница новая.**

Что прогон нашёл и что с этим сделано:

- **Тестов не было вовсе** у шаговой арифметики (`transitions.ts`, 31
  выживший), у перевода скорости пальца в полосы (`track.ts`), у фазы
  отпускания и конечной ветки `END_DRAG` (`reducer.ts`, `dragRelease.ts` — там
  же были и все 4 мутанта «без покрытия»), у `isJumping`, у синхронизации
  конфига и мгновенного режима в хуке. Заведено: новый файл
  `state/tests/transitions.test.ts` и блоки в девяти существующих.
- **Тесты были, но не проверяли то, что называли.** `dataKey` проверялся на то,
  что он МЕНЯЕТСЯ, и ни разу — на то, что РАЗЛИЧАЕТ; разделитель «проверялся»
  парой колод, которые различались и без него; клоны доклада проверялись на
  различность ключей, которую даёт любая пара чисел. Переписаны на конструкции,
  где ключи реально схлопываются.
- **Мёртвый код, который прогон и вскрыл.** Убраны: конъюнкт по `pageCount` в
  двух стражах `layout.ts` (делится на `visibleSlidesCount`, а `clamp`
  тотален), собственная защита `normalizePageIndex` поверх и так тотального
  `mod`, сравнение `pageCount` в `sameLayout` (выводится из остальных трёх
  полей), `pageDelta` в конечной ветке `resolveMoveTarget` (никто не читает),
  конъюнкт `&& goToPlan` (артефакт сужения типов) и `currentDirection !== 0`
  (ранний возврат выше уже это гарантирует — и после снятия конъюнкта стал
  нести нагрузку, что закреплено тестом).

Инструментальное, попутно: ESLint и Prettier читали песочницу Stryker
(`.stryker-tmp`) как исходники — 353 ошибки линта на сгенерированной копии
дерева. Обе папки (`.stryker-tmp`, `reports`) добавлены в игноры.

## 5. Интеракционные тесты жеста и движения

Логика движения закрыта, а **поведение** — нет: что колода трогается с той
позиции, где палец её оставил, что повторный клик не уводит дальше двух
страниц, что посадка приходит туда же, куда целились. Сегодня это проверяется
глазами на стенде.

Стенд для этого есть: часы движения детерминированы (`motion/runtime/clock.ts`),
кадровый цикл управляем, а `usePointerSwipe` принимает синтетические указатели.
То есть сценарий проигрывается без реального времени и без браузера — это
дешевле скриншотов и не мигает.

- Область: `client/gesture/**` + `client/motion/**` как связка, а не по файлам.
- Что закреплять: сценарии из `05-flows.md`, включая:  автоплей; клик, свайп - из состояния покоя; повторный клик, повторный свайп в направлении движения и в противоход движению - во всех сценариях когда начальное движение началось от клика, свайпа, автоплея;  дальний переход с телепортом и без (на количество станиц менее телепорта).Возможно,переход в обе стороны через границу "ленты данных", то есть смена с последней странцы на первую и наоборот в бесконечном режиме.  По одному тесту на сценарий.
- Чего НЕ закреплять: пиксели и тайминги кадров. Это скриншотный стенд, он
  дороже и отдельно (см. оценку готовности: третьим приоритетом, не первым).

Ожидаемый эффект: рефактор в `motion`/`gesture` перестаёт опираться на «прогнал
глазами» — сейчас это единственный способ убедиться, что поездка не изменилась.


*в 05-flows.md нет ничего про повторный свайп и про просто отопускание пальца на экран в процессе движения -  не знаю нужно ли это или нет. то есть это упущение базы, или там та же механика и оно не стоит выноса в отдельный раздел? надо проверить,и по необходимости исправить. так же проверить остальные сценарии которые я самостоятельно добавил в раздел  "Интеракционные тесты жеста и движения - область" - нужны ни отдельные упоминания и описания для них в 05-flows.md? то есть опять же - это недоработка базы, или  это одинаковые по сути процесса  сценарии и они не нуждаются отдельного пункта описания (он просто будет дублироваться)?