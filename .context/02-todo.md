# 02-todo — отложенное

Что решено сделать, но ещё не сделано. Не «идеи на будущее»: сюда попадает
только то, у чего есть согласованное решение и место в коде. Закрытый пункт
удаляется отсюда и, если он что-то меняет в устройстве проекта, переезжает в
[`01-facts.md`](./01-facts.md) или [`00-map.md`](./00-map.md).

## 1. Пять узлов под `react-hooks/refs` (задача 3(b))

Правило выключено для шести файлов в `eslint.config.js` с пометкой PENDING.
Разбор идёт по плану, порядок — от дешёвого к ядру.

| # | Узел | Что делаем | Риск | Статус |
| --- | --- | --- | --- | --- |
| 5 | `client/state/useCarouselState.ts` | **решено: вариант (b)** — `config` и `isInstantMode` переезжают в состояние редьюсера, конверт `ReducerEnvelope` уходит, `dispatch` становится стабильным без ref | высокий: ядро + тест редьюсера на 548 строк | не начато |

К пункту 5 пишется ADR (следующий свободный номер — 0004, язык английский:
файл лежит внутри `src/**`).

Закрыто: `compositedRide` (оба форка), `useSlideFetchReach`,
`useSlideRenderModel`, `useCarouselPresentation` — см. `01-facts.md`, A2.

Общее у всех пяти: межрендерное состояние лежит в ref, который пишут во время
рендера, а у первых трёх — внутри `useMemo`. React прямо требует от `useMemo`
чистоты и оставляет за собой право выбросить кэш; опираться на то, что он этого
не делает, — не гарантия, а везение. Это единственная часть группы, которая
является долгом сама по себе, а не «строгостью правил под компилятор».

Контроль на каждом шаге: `laneStability`, `imageRetention`, `renderWindow`,
`useSlideRenderModel`, `useSlideFetchReach`, полный прогон и сверка размеров
бандла с базовой линией из `01-facts.md`.

## 2. Снять PENDING после 3(b)

Когда пять узлов разобраны — убрать блок исключений для `react-hooks/refs` из
`eslint.config.js` и отразить это в `01-facts.md` (раздел A2).

## 3. Открытое решение: включать ли React Compiler

Сейчас он **не включён** (React 19, `@vitejs/plugin-react` без
`babel-plugin-react-compiler`). Правила группы React Compiler держатся ради
готовности к нему. Если решение — «не включаем никогда», это надо записать
в `01-facts.md` как решение заказчика, а часть работы 3(b) теряет смысл:
например, кэш полос в `useCarouselPresentation` существует ровно для того,
что компилятор делал бы сам.
