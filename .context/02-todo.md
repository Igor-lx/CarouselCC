# 02-todo — отложенное

Что решено сделать, но ещё не сделано. Не «идеи на будущее»: сюда попадает
только то, у чего есть согласованное решение и место в коде. Закрытый пункт
удаляется отсюда и, если он что-то меняет в устройстве проекта, переезжает в
[`01-facts.md`](./01-facts.md) или [`00-map.md`](./00-map.md).

## 1. Открытое решение: включать ли React Compiler

Сейчас он **не включён** (React 19, `@vitejs/plugin-react` без
`babel-plugin-react-compiler`). Правила группы React Compiler включены целиком и
соблюдаются — задача 3(b) закрыта. Остаётся решить, включать ли сам компилятор.
Если решение — «не включаем никогда», это надо записать в `01-facts.md` как
решение заказчика; тогда два кэша идентичности (`slideCache`, `laneCacheRef`)
остаются навсегда, а с компилятором они стали бы не нужны — он мемоизирует это
сам.
