# 08-tests — что пиннит каждый тест

Не «что тестируется», а **что закричит, если сломать**. Для рефактора это
главный вопрос: какой файл упадёт и о чём это скажет.

**Заполняется послойно.** Ниже — разобранные слои.

## I. `client/{presentation,context,host-report}/tests/**` — 4 файла / 378 строк

| Файл | Что закреплено |
| --- | --- |
| `context/tests/useModuleContextValue.test.tsx` (168) | **разделение половин**: стабильная переживает обычный ре-рендер, переход движения (тест назван «the whole reason for the split») и посадку, но пере-идентифицируется, когда в ней действительно что-то изменилось или открылись ворота предзагрузки; высокочастотная меняется на смену фазы и цели и стоит на месте, когда ни то, ни другое не двигалось |
| `presentation/tests/laneStability.test.tsx` (102) | идентичность стилей полос: перестроенный список слайдов с теми же полосами переиспользует **те же объекты**; вошедший слайд получает свой, соседи сохраняют свои; ре-базировка опоры сбрасывает кэш целиком; полосы остаются верными после ухода и возвращения слайдов в окно |
| `presentation/tests/presentation.test.ts` (65) | контракт JS→CSS: публикуются ровно объявленные переменные, тайминги форматируются как CSS-время, число слайдов проходит числом; на слайд — только полоса, и она относительна опоре; классовая карта отдаёт все ключи и подставляет пустую строку вместо отсутствующего; флаги стампятся только активные, с префиксом `data-` |
| `host-report/tests/statusSnapshot.test.ts` (43) | сравнение снимков реагирует на **каждое** объявленное поле — перебором, а не парой примеров |

## H. `client/{gesture,autoplay,focus}/tests/**` — 6 файлов / 929 строк

| Файл | Что закреплено |
| --- | --- |
| `useCarouselGesture.test.tsx` (207) | порядок команд: `START_DRAG` всегда раньше `END_DRAG`, старт придерживается ровно на одну задачу, стартов ровно один при любом входе в драг; исчезновение поверхности посреди жеста закрывает драг, но не выдумывает `END_DRAG` там, где драга не было; при выключенном свайпе слушателей нет вовсе; на слишком короткой колоде адаптер инертен; размонтирование снимает отложенный старт |
| `slotAdaptiveSwipe.test.ts` (148) | перевод настройки под слот: до замера — валидный конфиг на полу; хост-относительный порог движка выключен; в точке калибровки (400 px) кривизна равна базовой; резина масштабируется **обратно** слоту, флик — **прямо**; дистанция коммита зажата эргономическими границами; все непереводимые поля движка проходят насквозь |
| `useAutoplay.test.tsx` (236) | тик и его ворота: шаг по интервалу, ничего до срока, **одна активация — один тик**, следующий приносит цикл паузы; на конце колоды — возврат в начало; ровно один таймер в ожидании и ноль после; отключённый и приостановленный не взводят ничего; пауза посреди ожидания отменяет тик, а не откладывает; занятый вьюпорт перевзводит, вечно занятый не тикает и не течёт; ховер — пауза только после задержки, короткий ховер не паузит, уход возобновляет, `ignoreHover` делает указатель безразличным, выключение автоплея **отпускает** паузу, а не защёлкивает её |
| `useCarouselAutoplay.test.tsx` (170) | склейка: шаг вперёд с причиной `autoplay`, возврат домой на конце; ворота — хост не просил, колоде некуда идти, палец на ленте, собственная поездка (шаг не садится на шаг), возобновление после посадки; ховер на тач-колоде игнорируется |
| `coast.test.ts` (49) | доводка: экстраполяция по скорости и разрыву коммита, зажим **на** цели при пересечении, спокойное отпускание и откат доводки не получают, зажим интервала на `maxCoastMs`, обратный ход часов не даёт ничего, симметрия в обе стороны |
| `useFocusRecovery.test.tsx` (119) | **когда** спасать фокус: на посадке новой страницы — да; во время движения — нет; повторно на той же странице — нет; смена страницы без выхода из покоя (шаг в reduced-motion) — да; фокус вне карусели не трогается; размонтированный контейнер не роняет. Полоса проверяется через настоящий DOM |

## G. `client/geometry/tests/**` — 2 файла / 477 строк

| Файл | Что закреплено |
| --- | --- |
| `trackBinding.test.tsx` (353) | покадровая запись в пикселях слота; CSS-переход выключается один раз; повторный тот же `transform` не пишется; **общее правило сброса кадров** — палец красит каждый кадр, фолбэк роняет каждый N-й, флаг не протекает в следующий драг; владение компоузитором — берёт поездку при известной геометрии, отказывается при неизмеренном слоте и на вырожденных входах, блокирует покадровую запись, пропускает только ре-базировку, будит источник кадров на отмене, возобновляет краску после; **сдвиг базы под поездкой** — снос и ре-пин при смене origin, тишина когда origin не двигался, снос при смене слота |
| `slotSizeSource.test.tsx` (124) | два вида значения (живое сырое и округлённое состояние); ссылка на источник стабильна на рендере, который слот не двигает; подписчик получает уведомление, когда слот реально сдвинулся; подписка не пересобирается на пустом рендере; повторное измерение с тем же результатом не будит никого |

## E–F. `client/motion/tests/**`, `client/visual-position/tests/**` — 5 файлов / 1127 строк

| Файл | Что закреплено |
| --- | --- |
| `segmentFactory.test.ts` (436) | **механика, а не тюнинг** — знобы отпускания и `durationStep` в шапке пиннятся намеренно, чтобы ручная подстройка живых значений эти тесты не роняла. Пол длительности поездки: резкий флик на коротком остатке не схлопывается ниже пола, но запуск, который сам быстрее пола, **не замедляется**; темп шага по причине движения (клик против автоплея); непрерывность запуска жеста — старт с визуальной скорости, а не с более быстрой «памяти жеста», и отдельный блок про микро-удержание перед отрывом; потолок времени GO_TO — самый широкий ещё едущий переход упирается ровно во время перелёта, монотонность длительностей и стык «поездка/перелёт», выключенный телепорт снимает потолок совсем |
| `useMotionRunner.test.tsx` (304) | когда раннер **перепланирует**, а когда нет: тот же state — ни одного ре-плана, смена цели — ре-план, флип флага повторного клика — ре-план; что он **публикует** в каждой ветке (follow под пальцем, follow-fallback при отказе компоузитора, instant с направлением, waapi с кривой/часами/целью); дальний GO_TO получает план на **всю** команду, подход помечен как продолжение; план публикуется **на тех же часах**, что отданы компоузитору; контроллер прогоняет сегмент даже при композиции; отмена компоузитора и контроллера при размонтировании |
| `timing.test.ts` (181) | геометрия GO_TO как формула: зоны выводятся из настроек для любого размера страницы; дистанция подхода **не зависит от размаха**; ворота телепорта — перебор по всем размахам: пока нечего пропустить, колода едет; как только появляется пропускаемая страница, летит; порог считает **промежуточные** страницы; поднятый порог отодвигает перелёт; порог ниже структурного пола ничего не ломает; мастер-выключатель `goToTeleportEnabled`; время перелёта = преflight + подход, и разгон с хода укорачивает только преflight |
| `planChannel.test.ts` (69) | монотонность `planId`, доставка и отписка, дедуп: подряд идущие `idle` и одинаковые по флагу `follow` схлопываются, `waapi` — никогда, `follow` с разным флагом фолбэка — тоже нет |
| `useVisualPosition.test.tsx` (137) | кадр несёт позицию и в единицах колоды, и в страницах; смена размера страницы **перевыражает ту же позицию**, а не двигает её; новому подписчику текущий кадр отдаётся, если не сказано обратное; серия `running` — ноль на кадрах покоя, правило сброса не трогает покой и роняет ровно каждый N-й, но никогда первый |

**Чего не закреплено:** `useCarouselMotionExecution` собственного теста не имеет
— обёртка проверяется только через раннер.

## D. `client/state/tests/**` — 5 файлов / 1086 строк

| Файл | Что закреплено |
| --- | --- |
| `reducer.test.ts` (603) | вся механика команд. Стартовое состояние; MOVE в цикле (вперёд, назад с оборотом, причина автоплея) и в finite (клампы на обеих границах, отсутствие движения на краю); GO_TO — короткий прыжок без телепорта и дальний с ограниченным преflight'ом плюс отложенной целью; **направление GO_TO по шкале точек** — два теста, включая случай, где цикличный обход короче; MOTION_SETTLED — no-op в покое, посадка шага, разрез середины после преflight'а, посадка подхода, переякоривание при смене цели на лету; START_DRAG/END_DRAG — фиксация жеста, коммит в шаг, откат в snap, мгновенная посадка «уже на цели»; повторный клик — ретаргет на две страницы вперёд от **визуальной** позиции, удержание цели во время очереди, разворот в обратную сторону, **бёрст из 50 кликов не уводит колоду больше чем на две страницы**, автоплей под это правило не подпадает, первый клик после посадки снова обычный; мгновенный режим |
| `useCarouselState.test.tsx` (138) | обвязка, которой не видит редьюсер: **идентичность `dispatch` одна на всю жизнь компонента**; редьюсер получает текущий конфиг, а не прошлый; смена раскладки согласуется **до** команды; возвращаемое состояние никогда не отстаёт от пропа на раскладку; смена колоды — жёсткий сброс |
| `reconcile.test.ts` (179) | три исхода согласования: та же ссылка → то же состояние; структурно равная → подмена только ссылки с сохранением фазы; жёсткий сброс по `dataKey` и по `isFinite`; пропорциональный перенос с коллапсом движения в мгновенную досадку. Отдельно — **идемпотентность (контракт ADR-001)** таблицей из двух случаев; что `image` слайда не входит в `dataKey` (поворот экрана не сбрасывает позицию); и выход из «залипшей» фазы `dragging`, когда колода перестала скользить |
| `validateState.test.ts` (117) | три инварианта валидатора, причём фазовые — **таблицей по всем не-`step-jump` фазам**, а не парой примеров; пустая колода валидна вакуумно; независимые нарушения собираются все разом |
| `layoutBuilder.ts` (49) | не тест, а общая фикстура слоя: `makeLayout`, `makeState`, список `NON_JUMP_PHASES`. Комментарий в шапке объясняет, почему она одна на слой: дублировать её по файлам — дублирование внутри одной единицы кода, то есть дефект |

**Чего в слое не закреплено:** ветка `direction === 0` в `MOTION_SETTLED`
(«вырожденная») собственного теста не имеет.

## B. `client/config/**` — собственных тестов нет

Слой не покрыт ни одним тестом напрямую, и это не пробел: проверять в нём нечего,
кроме сборки объекта. Значения косвенно закреплены в двух местах:

- `modules/Diagnostic/checks/constantChecks.ts` и его тесты — сверка
  опубликованных значений с реальными;
- `client/tests/carouselContract.test.tsx` — держит `REPEATED_CLICK_VISUAL_LOOKAHEAD_PAGES`
  вместе с поведением, которое от него зависит.

Что **не** закреплено ничем: `withDefault` пропускает `-1`, `0`, `NaN` и строку
там, где ждут число. По ADR-002 это осознанно, но тестом не зафиксировано —
значит и не заметит, если однажды кто-то решит «добавить валидацию».

## C. `client/domain/tests/**` — 9 файлов / 873 строки, ни одного рендера

| Файл | Что закреплено |
| --- | --- |
| `math.test.ts` (28) | `mod` на отрицательных и `total <= 0`. Прямо сказано, что `clamp` намеренно **не** тестируется: он проверяется через результат у вызывающих |
| `layout.test.ts` (173) | геометрия страниц целиком: клампинг размера страницы, округление последней страницы вверх, `virtualLength` в цикле против finite, `dataKey` меняется по идентичности слайдов и не меняется от размера страницы, пустая колода без деления на ноль. Отдельный блок — «`pageContaining` (floor) против `nearestPageIndex` (round)», включая тест «они расходятся намеренно». Плюс `alignedVirtualIndex` (ближняя полоса, не нулевая) и пропорциональный `reconciledPageIndex` |
| `renderWindow.test.ts` (129) | буферное окно **всегда** содержит сегмент (перебор пяти пар, включая дробные и обратные), буфер по целым экранам с каждой стороны, цикл не клампится, finite клампится в данные, нескользящая колода рендерит саму себя, пустая даёт вырожденное окно. `expandWindow` только расширяет — это и есть «слайд не пропадёт посреди поездки» |
| `visibility.test.ts` (90) | дробное удержание оставляет активными **все** слайды на экране, для N = 1…6; `isActual` при этом остаётся ровно один — `aria-current` не размывается; во время поездки активны и стартовая, и целевая страницы |
| `deckPadding.test.ts` (112) | доклад до целых страниц: уникальность ключей (перебор пяти колод), клон переиспользует **данные**, но не идентичность, `layoutIndex` остаётся плотным рядом (на нём держится `aria-label`), докладу никогда не нужно больше фиксов, чем слайдов (перебор 12×12), и — идентичность массива при целой колоде, чтобы не рвать мемо потребителей |
| `dragRelease.test.ts` (112) | контракт отпускания: от покоя решает геометрия; при перехвате летящей поездки — **нажатая** страница, а не ближайшая, и это не snap, а нормальный шаг; без замера нажатия — цель поездки; явный свайп перебивает всё |
| `measureSlotSize.test.ts` (73) | единственный тест слоя с `jsdom`: слот = (ширина + зазор) / число слайдов; отсутствующий и непарсящийся зазор дают ноль, а не `NaN`; явно переданная ширина не заставляет перечитывать бокс; ноль слайдов и схлопнутый вьюпорт дают 0; фолбэк на `--gap` |
| `renderedImageSrc.test.ts` (122) | выбор URL: самый широкий кандидат по `w`, сканируются **все** наборы, при равной ширине выигрывает первый (default), в single-set режиме приоритет у `defaultSrc` издателя, не-строковый `content` картинкой не считается |
| `track.test.ts` (34) | трансформ считается от `layoutOrigin`, а не от нуля: одинаковая разница даёт одинаковый сдвиг; полоса инвариантна к сдвигу опоры — на этом держится «редкая ре-базировка никого не двигает» |

**Чего в слое не закреплено:** `laneDistanceFromBand` не имеет собственного
теста — его поведение проверяется только косвенно, через потребителей
(`useSlideFetchReach`).
