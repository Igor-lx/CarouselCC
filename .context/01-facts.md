# 01-facts — то, чего нет ни в коде, ни в документации

Дополнение к [`00-map.md`](./00-map.md). Там — что делает каждый файл. Здесь —
**механически проверенные факты о проекте как о целом**. Всё получено скриптами,
не чтением.

## Что здесь читают целиком, а что смотрят по адресу

Файл длинный, и читать его весь перед каждой правкой не нужно. Целиком — только
то, без чего можно начать работу неверно; остальное открывают, когда вопрос
возник.

| Читать целиком, до работы | Зачем |
| --- | --- |
| «Как читать пути» | иначе сокращения в карте ведут не туда |
| A. Базовая линия | что считается зелёным и с какими числами сравнивать |
| B. Цикл импортов | их ровно один, и он ловушка при переносе файла |
| H. Решения по объёму | что уже решено и не обсуждается заново |
| J. Как перепроверить | какие команды вообще существуют |
| K. Ловушки окружения | каждая уже стоила времени |

| Смотреть по адресу | Когда |
| --- | --- |
| A2. Инструменты проверки | правишь конфиг линта или трогаешь `eslint-disable` |
| C. Радиус поражения | нужен масштаб правки — но живее `graph.mjs blast` |
| D. Мёртвые экспорты | удаляешь или переименовываешь экспорт |
| E, F. Дрейф и заявки доков | трогаешь `docs/**` или опираешься на их утверждение |
| G. Указатель документации | ищешь, где про это написано подробно |
| I. Проход «вырезать историю» | встретил ссылку на прошлые прогоны |

Разделы C и D дублируют то, что `graph.mjs` считает заново: в тексте они полезны
как картина, но при расхождении прав скрипт.

## Как читать пути в этом документе и в `00-map.md`

Пути сокращены и лежат на **разной** глубине — дописывать один общий префикс
нельзя:

| Сокращение | Реальный путь |
| --- | --- |
| `client/**` | `src/components/Carousel/client/**` |
| `docs/**` (architecture, adr, config) | `src/components/Carousel/client/docs/**` |
| `boundary/**`, `data-gen/**` | `src/components/Carousel/**` |
| `shared/**` | `src/shared/**` |
| `app/**`, `main.tsx`, `globals.scss` | `src/**` |
| `public/**`, `package.json`, `tsconfig*.json` | от корня репозитория |

⚠️ `docs/` — **не** в корне репозитория, а внутри `client/`. Якоря в коде
(`// See docs/architecture/motion.md`) указывают именно туда.

Отсюда и относительные импорты в коде: `../../../../shared` из
`client/motion/*` — это `src/shared`, а не папка внутри карусели.

---

## A. Базовая линия

Замерена целиком на текущем состоянии — не унаследована.

| Проверка | Команда | Результат |
| --- | --- | --- |
| Типы | `npm run typecheck` | **exit 0** |
| Тесты | `npm test` | **153 файла / 1892 теста — все зелёные** |
| Смоук в браузере | `npm run test:e2e` | **6 проверок, все зелёные**, ~22 с; вне `npm test` |
| Сборка | `npm run build` | **exit 0**, 229 модулей |
| Линт | `npm run lint` | **exit 0** |
| Формат | `npm run format:check` | **exit 0** |
| База знаний | `node .context/graph.mjs verify` | **exit 0** — ни одна сверка из таблицы § J не провалилась |
| Покрытие | `npm run test:coverage` | строки **97.67 %** (4192/4292), ветки **93.05 %** (2371/2548), функции **96.18 %** (907/943), инструкции **96.61 %** (4595/4756) |
| Мутации | `npm run test:mutation` (вся область) / `npx stryker run --mutate "<пути>"` (по файлам правки, часть закрытия узла) | **83.87 %** по всей отгружаемой области одним прогоном: убито 4695, таймаутов 17, выжило 857, без покрытия 47, ошибок 2 из 5618 засчитанных; живых **906**. Строковых мутантов 469 — помечены `Ignored`, в знаменатель **не входят**. 123 файла, тестовых среди них ноль. Прогон ~72–75 минут; повторный замер на том же коде дал те же числа до единицы. Накопительный реестр замеров — `.context/mutation-ledger.json`, 131 запись, непромеренного нуля; долг спрашивают у `graph.mjs mutated`, а не у последнего отчёта. По слоям: домен и состояние 96.8 %, движение клиента 91.1 %, ядро движения 90.3 %, движок жеста 83.3 %, DOM и геометрия 82.8 %, полки состояния 80.6 %, модули 75.7 %, жест клиента 72.6 %. Выжившие разобраны поимённо по областям: `08-tests.md` — § E (домен и состояние), § G (ядро движения), § H2 (жест), § H3 (DOM и геометрия), § H4 (обвязка и презентация), § H5 (пагинация) |

Покрытие меряется по всему, что уезжает в бандл, — и карусель, и полки, и
`.tsx`. До этого замера конфиг исключал `src/shared/**` целиком и все `.tsx`:
инструмент считал 2506 операторов вместо 4695, то есть описывал половину кода
числом, которое читается как целое. Процент при этом почти не изменился
(86.43 → 86.36 по операторам) — полки покрыты не хуже компонента, но узнать это
было нельзя, пока их не мерили.

Время сборки в эталон не входит: оно скачет от прогона к прогону на одной и той
же машине и меряет её, а не проект.

**Размеры прод-бандла (эталон для сравнения после правок):**

| Артефакт | Размер | gzip |
| --- | --- | --- |
| `dist/assets/index-*.js` | 283.92 кБ | **89.11 кБ** |
| `dist/assets/index-*.css` | 13.48 кБ | 3.28 кБ |
| `dist/index.html` | 1.81 кБ | 0.85 кБ |

**Демо-данные (`public/carousel-slides1.json`):** 12 слайдов, `id` — UUID,
`content` уникальны, у каждого `image.srcSet` (480/720) + `defaultSrc` (720) +
один art-directed `<source>` на `short-landscape` (480/720/1080/1600).
⚠️ **`alt` пуст у всех 12** — генератор скаффолдит `""` и ждёт ручного
заполнения (`data-gen/README.md:52`). Следствие в рантайме: каждая картинка
декоративна для AT, а при ошибке загрузки слайд показывает
`errAltPlaceholder` («Downloading Error»), а не описание.
Второй набор (`carousel-slides2.json`) устроен так же.

**Ловушка №1 (записана в самом `tsconfig.json:1-7`):** корневой конфиг —
solution-style, `files: []`. `tsc --noEmit` в корне проверяет **ноль файлов** и
выходит с кодом 0. Зелёный результат, который ничего не доказывает. Проверять
только через `npm run typecheck` (`tsc -b`, собирает четыре референса) или
`npm run build`.

**Четыре проекта TS:** `tsconfig.app.json` (src минус data-gen минус тесты),
`tsconfig.node.json` (vite/vitest/playwright-конфиги + data-gen, lib ES2023 **без
DOM**), `tsconfig.test.json` (наследует app, только тесты, + типы node/vite),
`tsconfig.e2e.json` (только `e2e/`, lib ES2023 + **DOM**). Четвёртый заведён
отдельным, а не подшит к node-проекту, ради одной строки: колбэки
`page.evaluate` исполняются в браузере и требуют `DOM`, а открыть её node-проекту
значило бы открыть и генератору данных, чья изоляция от браузера — записанное
свойство (`09-decisions.md`, § H).

**Строгость, объясняющая форму кода:** `strict`, `noUncheckedIndexedAccess`
(отсюда `!` после индексации), `exactOptionalPropertyTypes` (отсюда
`?: T | undefined` вместо `?: T`), `noUnusedLocals`/`noUnusedParameters`
(отсюда `void x` в тестах), `erasableSyntaxOnly`, `noFallthroughCasesInSwitch`,
`verbatimModuleSyntax` (отсюда `import type` везде).

---

## A2. Инструменты проверки (заведены после базовой линии)

| Файл | Что задаёт |
| --- | --- |
| `stryker.config.json` | мутационный прогон: область — **весь прод-код клиента и полок**. Сколько в ней файлов, спрашивают у `graph.mjs mutated`, а не у этой строки: замороженное здесь число сдвигается от каждого нового узла и разошлось молча уже однажды. Вне области ровно четыре группы, каждая с причиной: фасад `engines/kinetic` — объединённая копия двух форков, гарантия приходит из движков по смыслу (`09-decisions.md`, § G); dev-слой `modules/Diagnostic` — не уезжает в прод-бандл (`08-tests.md`, § F); таблицы констант `client/config/*` — их мутанты убиваются только пересказом самих значений; демо-стенд и генератор его данных — не уезжают пользователю (`09-decisions.md`, § H). Строковые мутанты исключены мутатором и помечаются `Ignored`: **в знаменатель счёта не входят**. Раннер vitest, анализ покрытия `perTest`, порог не рвёт сборку (`break: null`) — это аудит, а не ворота. Сверка «что в области, что вне» делается перебором `src`, а не по памяти |
| `package.json` → `overrides: { "qs": "^6.16.0" }` | **не удалять без проверки `npm audit`.** `@stryker-mutator/core` тянет `typed-rest-client`, тот прибивает `qs@6.15.1`, а уязвимы все `qs` до 6.15.3 включительно. Диапазон стрykера (`~2.3.0`) чистой версии клиента не оставляет: всё, что `>=2.3.1`, помечено — и помечено **ровно за эту прибитую зависимость**. Override поднимает `qs` до 6.16.0, то есть чинит причину, а не симптом, и без даунгрейда. Проверено: `npm audit` даёт ноль, stryker после этого отработал (88.00 % на `useOffsetFollow.ts`, те же 3 живых, что в полном прогоне) |
| `@playwright/test` (dev-зависимость) | управление настоящим Chromium: снимки, чтение DOM, эмуляция устройств, троттлинг CPU, трассировка композитора через DevTools Protocol. С этого захода на ней стоит **узкий смоук в настоящем движке** (`npm run test:e2e`, 6 проверок, ~22 с) — он **не входит** в `npm test`: vitest собирает только `src/**`, а спеки лежат в `e2e/`. Файлы: `playwright.config.ts` (поднимает **собранный** бандл через `preview`, а не дев-сервер — у того в конфиге `server.open`), `e2e/motion.spec.ts`, `tsconfig.e2e.json`. Разбор — `08-tests.md`, § Браузер. Всё остальное по-прежнему разовые замеры и осмотр глазами. Браузеры лежат в общем кэше `%LOCALAPPDATA%ms-playwright` (703 МБ: chromium 428, headless shell 272, ffmpeg 3.4) — он **вне репозитория и общий для всех проектов**, поэтому в новом проекте нужен только `npm i -D` |
| `eslint.config.js` | flat config: `typescript-eslint` с типовой проверкой (`projectService`), `react-hooks`, `react-refresh`; форматирование целиком отдано Prettier через `eslint-config-prettier` |
| `.prettierrc.json` | `printWidth: 80`, двойные кавычки, `trailingComma: "all"`, `endOfLine: "auto"` |
| `.prettierignore` | `dist`, `coverage`, `node_modules`, `public`, `package-lock.json`, а также **все `*.md`**: markdown здесь — свёрстанная руками проза, Prettier только доливает в неё пустые строки |
| `.gitattributes` | `* text=auto eol=lf` — концы строк перестали зависеть от `core.autocrlf` машины (на этой он `true`, файлы в рабочем дереве были CRLF) |

**Три обоснованных исключения в конфиге линта, все с комментарием на месте:**

1. `@typescript-eslint/require-await` выключен в `**/tests/**`: React-`act`
   возвращает thenable только при async-скоупе, поэтому `async` без `await`
   внутри — требование API, а не оплошность.
2. `react-refresh/only-export-components` выключен в `client/modules/**`:
   слот-компоненты собираются как `Object.assign(Component, { slot })`, Fast
   Refresh такую форму не отслеживает, а форма — это контракт модуля.
3. `react-hooks/globals` и `react-hooks/refs` выключены в `**/tests/**`:
   фикстуры живут на модульном уровне и переприсваиваются между кейсами, а ref
   тест читает и подменяет намеренно. Оба правила описывают код компонентов.
4. PENDING больше нет: правило включено везде. Было шесть файлов.

**Что разобрано из PENDING и как:**

| Узел | Решение |
| --- | --- |
| `compositedRide` (оба форка) | райдер держится в состоянии: он владеет живой ручкой анимации, а memo/ref из рендера для этого не годится; смена контроллера — правка состояния во время рендера |
| `useSlideFetchReach` | стабильная ссылка на URL полосы — в состоянии с чистой сверкой `sameUrls`, вместо ref, который писали внутри `useMemo` |
| `useSlideRenderModel` | окно рендера и `layoutOrigin` — явное состояние с правкой во время рендера. Побочная выгода: идентичность окна теперь меняется только при смене границ, раньше на каждом рендере в покое рождался новый объект |
| `useCarouselPresentation` | кэш полос переехал в ref с ключом `origin:index`, поэтому синхронная инвалидация не нужна вовсе; чистка — в эффекте после коммита |
| `useCarouselState` | контекст редьюсера (`layout`, `config`, `isInstantMode`) переехал **в состояние**, конверт `ReducerEnvelope` удалён, фиксация — командой `SYNC_CONTEXT` во время рендера. Рефов в файле не осталось. См. `docs/adr/0004-reducer-owns-its-context.md`; он же поправляет ADR-001 (границ согласования теперь одна, а не две) |

**Точечные `eslint-disable` — все пять, посчитаны грепом:**

| Файл | Правило | Почему |
| --- | --- | --- |
| `slides/useSlideRenderModel.ts` ×2 | `react-hooks/immutability` | кэш идентичности `VirtualSlide`. **Замерено:** перерисовок не предотвращает ни одной — экономит аллокации. Судьба открыта, `02-todo.md`, пункт 4 |
| `slides/imageResource/useImageResourceStoreInstance.ts` ×2 | `react-hooks/refs` | документированная React ленивая инициализация рефа; условное создание не выражается через `useState`, а создание из эффекта отдало бы `null` на рендер |
| `modules/Pagination/basic/tests/usePaginationFade.test.tsx` | `@typescript-eslint/unbound-method` | сохранение метода прототипа для восстановления; jsdom не имеет `Element.animate`, поэтому `vi.spyOn` не подходит |

Из списка ушли трое, и по одной причине: **исключение снимается вместе с
конструкцией, которая его требовала.** Кэш полос в `useCarouselPresentation` —
после переноса в ref с ключом `origin:index`. Оба `rules-of-hooks` и один
`exhaustive-deps` в медиа-полке — после того, как набор условий стал ездить на
одной подписке вместо хука на условие. `rules-of-hooks` в проекте не отключён
теперь нигде.

**Разбор группы React Compiler — выполнен целиком.** Было 110 срабатываний,
осталось **0**; правила включены во всём репозитории.

| Правило | Было | Стало | Чем закрыто |
| --- | --- | --- | --- |
| `immutability` | 4 | **0** | проброс потребительского ref вынесен в `assignHostRef` на уровень модуля (`usePointerSwipe`, оба форка) |
| `set-state-in-effect` | 3 | **0** | «нечего ждать» считается в рендере (`useSlideFetchReach`), защёлка буфера — правкой состояния во время рендера, сброс hover-паузы — тем же приёмом (`useAutoplay`) |
| `refs` (зеркала значений) | 20 | **0** | запись перенесена в эффект: `useMotionPaint`, `useKineticValue`, `usePointerSwipe`, `useViewportBusy` (все — оба форка, где есть) и в layout-эффект: `useTrackBinding`, `useSlotSizeSource`, `useVisualPosition`, `usePaginationFade` |
| `refs` (ленивый синглтон) | 6 | **2** | `useMotionController` переведён на инициализатор `useState` (оба форка); `useImageResourceStoreInstance` оставлен с точечным `eslint-disable` и обоснованием — условное создание нельзя выразить через `useState` |
| `refs` (состояние в ref из рендера) | 53 | **0** | окно рендера и опора — в состояние (`useSlideRenderModel`); ссылка на URL полосы — в состояние (`useSlideFetchReach`); кэш полос — в ref с ключом `origin:index` (`useCarouselPresentation`); райдер — в состояние (`compositedRide`, оба форка); контекст редьюсера — в состояние (`useCarouselState`, ADR-004) |
| `globals` | 24 | 24 | все в тестах, правило там выключено с обоснованием |

**React Compiler в проекте не включён** (React 19, `@vitejs/plugin-react` без
`babel-plugin-react-compiler`) — решение разработчика, H3. Правила группы дают
готовность к нему и гигиену конкурентного рендера, а не чинят сегодняшние
падения.

---

## B. Циклическая зависимость — ровно одна

```
client/motion/index.ts
  → motion/useCarouselMotionExecution.ts
  → motion/useMotionRunner.ts
  → client/geometry/index.ts
  → geometry/useTrackBinding.ts
  → client/motion/index.ts      ← замыкание
```

**Механика:** `useTrackBinding` берёт из бочки `../motion` значения
(`keyframesAlongStops`, `startPinnedAnimation`) и тип `MotionPlanSource`;
`useMotionRunner` берёт из бочки `../geometry` тип `TrackBindingApi`.

**Почему сейчас не ломается:** обратное ребро (`useMotionRunner → geometry`) —
**только тип**, он стирается при компиляции, поэтому в рантайме цикла нет.
**Что это значит для правок:** любая правка, превращающая этот импорт в
значение, замкнёт настоящий рантайм-цикл (порядок инициализации модулей,
`undefined` при первом обращении). Разрывается тривиально — импортировать
`keyframesAlongStops`/`startPinnedAnimation` в `useTrackBinding` напрямую из
`shared`, минуя транзитную бочку `client/motion`.

---

## C. Радиус поражения: что ломает больше всего

Числа здесь не переписываются руками — они считаются:

```
node .context/graph.mjs blast              # весь список, по убыванию
node .context/graph.mjs blast domain/index # кто именно зависит от одного файла
```

Раньше в этом месте лежала таблица, скопированная из вывода команды. Она
устарела молча (у `config/index.ts` было записано 16 импортёров при 17 на диске)
и не покрывалась ни одной проверкой `verify`. Дублировать вычислимое — заводить
второй источник истины, который расходится первым; поэтому осталась только
команда и то, что из вывода не следует, — **почему** точка горячая:

- `shared/index.ts` (первое место с большим отрывом) — единственная дверь
  наружу из полок. Ширина здесь не дефект: барьер форка держится именно тем,
  что компонент не знает внутренней раскладки полки.
- Бочки `domain`, `config`, `state`, `context`, `motion` — намеренные швы слоёв.
  Правка **внутри** такой папки радиус не имеет; радиус имеет правка её
  публичной поверхности, и это ровно тот случай, когда сначала смотрят `blast`.
- `public-api/types.ts` и `Diagnostic/types.ts` — типы, а не код: их радиус
  проверяет компилятор, и он же его чинит.

**Файлы, которые не импортирует никто (не-тестовый код):**

| Файл | Статус |
| --- | --- |
| `data-gen/index.ts` | программный API папки, объявленный в её шапке (`runDataGen` / `generateSlides` / `buildSlide`); по графу мёртв, существует ради переноса — тот же случай, что барьер форка |
| `data-gen/cli.ts` | точка входа npm-скрипта — ожидаемо |
| `main.tsx` | точка входа приложения — ожидаемо |
| `shared/viewportObservation/useIsomorphicLayoutEffect.ts` | «спящая» копия, помечена в коде и README — ожидаемо |
| `vite-env.d.ts` | ambient — ожидаемо |

---

## D. Мёртвые экспорты (проверено грепом, не эвристикой)

Разделены по смыслу — не всё «мёртвое» подлежит удалению.

### D1. Пусто — и это не случайность

Здесь был список транзитных реэкспортов, которые никто не импортировал: два в
`motion`, четыре в `presentation`, один в `slides`, три в `domain` и дубль
`default` в бочке диагностики. **Они удалены, а не описаны.** Раздел оставлен
пустым намеренно: место, куда можно положить известный дефект, рано или поздно
им наполняется. Найденное чинится, а сюда попадает только то, что осталось
жить по причине — и тогда ему место в D2 или D3.

Ждавшая решения `client/modules/index.ts` удалена: бочка была мёртвой и
неполной, а общий корень импорта для модулей ещё и опасен — он втянул бы
диагностику в граф каждого потребителя.

### D2. Мёртвое внутри форка

- `shared/clientState/media/useMedia/internal/useOrientation.ts` →
  **сам хук `useOrientation` не вызывается никогда.** Форку нужны только
  константы `PORTRAIT_ORIENTATION_QUERY` / `LANDSCAPE_ORIENTATION_QUERY`;
  вместе с хуком в форк тянется и импорт `useMediaQuery`.
- `shared/clientState/media/useMedia/internal/resolveActiveBreakpoint.ts` →
  `STANDARD_BREAKPOINTS` (в форке не нужен, оси задаёт карусель).
- `shared/engines/kinetic/internal/motion/index.ts` → 14 реэкспортов, которых
  фасад не касается; `internal/gesture/index.ts` → 4.
  **Это НЕ мусор:** барьер форка обязан быть полным, чтобы папка выносилась
  копированием. Трогать нельзя.

### D3. Публичная поверхность — не трогать

- **`public-api/types.ts` — сам контракт, а не только его бочка.** Здесь
  объявлены `CarouselProps`: каждое имя поля — то, что пишет хост у себя.
  Переименование пропа (`errAltPlaceholder` → что угодно) ломает всех
  потребителей и **компилятором у них не ловится** — их код просто перестаёт
  передавать то, что передавал. Радиус внутри репозитория проверит `tsc`, но
  радиус снаружи не проверит никто, поэтому такая правка выносится отдельно и
  согласуется. Раздел C выше говорит про этот файл «типы, а не код, радиус
  проверяет компилятор» — это про радиус **внутренний**, и одного этого читать
  недостаточно.
- `client/index.ts` и `public-api/index.ts` отдают оба набора ключей
  (`CLASS_NAME_KEYS`, `SLIDE_CLASS_KEYS`) и оба типа ключа. Значения живут
  через `public-api/types` напрямую, реэкспорты — контракт для хоста. Асимметрия
  (`CLASS_NAME_KEYS` был только в `public-api`, хотя `ClassNameMap` ключуется
  именно им) снята: точка входа пакета обязана отдавать ключи той карты, которую
  требует заполнить.
- `shared/theme/index.ts` → `ThemeStateProvider`, `THEME_MODES` — заявленный
  публичный API коробки (README описывает «Mode B» на `ThemeStateProvider`).
- Экспортируемые типы без потребителей (`GoToPlan`, `ModuleRenderPolicy`,
  `PaginationWidgetBinding`, `CoastedLaunchInput`, …) — часть читаемого
  контракта модуля.

---

## E. Дрейф документации — пусто

Здесь был список из пяти мест, где документация описывала не тот код:
несуществующая функция `syncGeometry` в geometry.md, «клонируются хвостовые
слайды» вместо головных в public-api.md, папки `core/` и `chrome/`, которых нет
в полке темы, и осиротевший заголовок в README генератора. **Все исправлены**,
а не описаны: список известной неправды — та же парковка для дефектов, что и
«известный мусор» (см. `CLAUDE.md`, «Найденный дефект чинится, а не
описывается»).

Раздел остаётся как место для находок **текущего захода**: нашёл расхождение —
чинится в этом же заходе, а сюда попадает только то, что почему-то отложено, и
тогда причина обязана быть названа.

## F. Утверждения доков, которые код не выражает сам

> **Статус этого раздела: заявки, а не факты.** Ниже — то, что документация
> проекта утверждает и чего из кода не видно. Часть заявок опирается на прошлые
> прогоны разработки и замеры, сделанные до нас. Мы их **не наследуем как
> авторитет**: каждая проверяется заново, когда до неё дойдёт дело. Держать в
> голове стоит не вывод, а **место, где он может выстрелить** — чтобы правка
> не прошла мимо непроверенного допущения молча.

- **ADR-001 + ADR-004** — `reconcileStateToLayout` обязан быть
  **идемпотентным**; это жёсткий контракт, закреплённый тестом. А вот «ровно две
  точки применения» из ADR-001 **отменено ADR-004**: согласование и фиксация
  сведены к **одной** границе, и в коде вызов ровно один (`state/reducer.ts`).
  Так говорят и статус самого ADR-001, и `state.md` («runs at exactly ONE
  boundary»); здесь до недавнего стояла старая редакция.
- **ADR-002** — все входы caller-owned. Никакой валидации/нормализации в
  проде **нигде**, включая перерасход долей профиля. Diagnostic только
  наблюдает.
- **ADR-003** — почему не `transition: transform`: (1) профиль accel/cruise/decel
  невыразим одним безье, (2) ретаргет теряет скорость, (3) нечего разделить с
  точками и виджетом и нет `startTime`, (4) `transitionend` теряется.
- **`shared/README.md:23-25`** — «каждая заготовка держит свои копии хуков; **то,
  что не дублируется никогда, — стор**». Стор — это папка `clientState/sharedStore/`
  целиком (`useMediaQuery` + `useMediaQuerySet`, второй импортирует первый).
  Читать это надо узко: правило про **одну копию этой папки на проект**, а не
  про запрет модульного состояния как такового. `useIsTouchDevice` и
  `useDataSaver` держат собственные синглтоны законно — их источники
  (`pointerdown`, `navigator.connection.saveData`) через media-запрос не
  выражаются. Разобрано в § H.2.
- **`shared/engines/gesture/README.md:21`** — «**Touch pointers only** (mouse/pen
  ignored)» — принцип движка, задокументирован. В `public-api.md` карусели этого
  нет (см. `00-map.md § S`).
- **`docs/architecture/viewport.md:36-41`** — `useSlideViewport` вызывается
  **только корнем**, это
  правило: каждый лишний `useMedia` добавляет по подписке на условие и пересборку
  `MediaState` на каждом экземпляре. Листья получают значение пропом.
- **`diagnostics.md:29-33`** — в файлах `checks/` **запрещены модульные
  side-effect'ы** (`const X = new Set(...)` на верхнем уровне): бандлер не
  докажет чистоту и оставит строки в проде. Таблицы строятся внутри коллекторов.
- **`styling.md:83-101`, `modules.md:56-67`** — «два эффекта на одном свойстве»:
  CSS-transition + WAAPI на opacity/transform роняют анимацию Blink на главный
  поток. Отсюда `transition: none` на время поездки и постоянный
  `will-change` у точек. Помечено «Do not remove».
- **`presentation.md:48-68`** — кэш стилей полосы существует, потому что
  `virtualSlides` пересобирается на каждом флипе `isMoving`, то есть дважды за
  поездку, в двух самых дорогих кадрах. **Механизм продемонстрирован** (не сам
  кэш полос, а именно он): контрольный опыт в
  `slides/tests/slideRenderCost.test.tsx` показывает, что проп-объект без кэша
  идентичности перерисовывает всю колоду. Сам кэш полос напрямую не замерялся —
  его тест (`laneStability`) пиннит идентичность, а не число рендеров.
- **`autoplay.md:58-64`** — `onStep`/`onGoToStart` обязаны быть стабильны:
  свежая идентичность перезаряжает таймер и отсчитывает интервал от рендера,
  а не от тика.

---

## G. Указатель документации (что где искать)

| Нужно понять | Файл |
| --- | --- |
| Владение, 5 SSOT, карта папок, порядок чтения | `docs/architecture/overview.md` |
| Публичный контракт, пропы, DOM, форма данных | `docs/architecture/public-api.md` |
| Редьюсер, разрешение шага, повторный клик | `docs/architecture/state.md` |
| Чистое ядро: страницы, окно, видимость, транс­формы | `docs/architecture/domain.md` |
| Сегменты, handoff, телепорт, компоузитор, полосы | `docs/architecture/motion.md` |
| SSOT видимой позиции, пропуск кадров | `docs/architecture/visual-position.md` |
| Измерение слота, запись трансформа, два владельца краски | `docs/architecture/geometry.md` |
| Свайп, слот-нормализация, коастинг, две скорости | `docs/architecture/gesture.md` |
| Окно рендера, две волны загрузки, стор картинок, вуаль | `docs/architecture/slides.md` |
| Оси вьюпорта, data-атрибуты, canonical media | `docs/architecture/viewport.md` |
| JS→CSS контракт, кэш полос | `docs/architecture/presentation.md` |
| Правила стилей и ловушки рендеринга | `docs/architecture/styling.md` |
| Разделение контекста по частоте | `docs/architecture/context.md` |
| Поведение каждого слот-модуля | `docs/architecture/modules.md` |
| Dev-слой, нулевая цена в проде | `docs/architecture/diagnostics.md` |
| Осознанные компромиссы | `docs/architecture/quality.md` |
| Значение каждой константы | `docs/config/*.md` (8 файлов) |
| Три решения с долгими последствиями | `docs/adr/000{1,2,3}-*.md` |
| Движки как самостоятельные продукты | `shared/engines/{motion,gesture,kinetic}/README.md` |
| Правило «стор не дублируется» | `shared/clientState/sharedStore/README.md` |
| Генератор контента, конфиг, идемпотентность | `data-gen/README.md` |
| Тесты границы client ↔ data-gen | `boundary/README.md` |

---

## H. Решения по объёму (приняты разработчиком)

1. **Стенд правим только по необходимости.** `App.tsx`, `App.module.scss`,
   `globals.scss`, `index.html`, данные в `public/` трогаем лишь тогда, когда
   этого требует правка в компоненте или на полках. Их собственные
   шероховатости идут отдельным списком «к сведению», не в план:
   - `App.tsx:35-40` — чтение `window.location.search` на модульном уровне
     (SSR-враждебно, вычисляется при импорте);
   - `App.module.scss` — сырые `@media` вместо осей (это хост, у него свои
     оси, — формально допустимо);
   - `alt: ""` у всех 12 слайдов в обоих `public/carousel-slides*.json`.

2. **ЗАКРЫТО — собственный стор у `useIsTouchDevice` и `useDataSaver` не дефект.**
   Ранее здесь стоял открытый вопрос: почему эти два хука держат модульный
   синглтон, а `useIsReducedMotion` делегирует в `sharedStore/useMediaQuery`.
   Разобрано, вопрос снят.

   `useMediaQuery` — мультиплексор «один слушатель на строку media-запроса», и
   больше он не умеет ничего. Два хука наблюдают источники, которые в
   media-запрос не выражаются:
   - `useIsTouchDevice` = `(pointer: coarse)` **ИЛИ** первый `pointerdown` с
     `pointerType === "touch"` (защёлка + снятие слушателя);
   - `useDataSaver` = `(prefers-reduced-data: reduce)` **ИЛИ**
     `navigator.connection.saveData` с её собственным событием `change`.

   `useIsReducedMotion` делегирует не потому, что «правильнее», а потому что он
   чистый media-запрос и ничего сверх. Всё это задокументировано таблицей в
   `environment/library/README.md:9-11`, включая цену делегирования: «rides
   `../../sharedStore/useMediaQuery` (**copy that folder too**)».

   Конвенция `shared/README.md` про **один `useMediaQuery.ts` на проект**, а не
   про запрет модульного состояния вообще. Собственный синглтон у хука с
   не-media источником её не нарушает.

   Замысел полок (подтверждён разработчиком): нужен один хук — копируешь только
   его, он самодостаточен; нужны все — берёшь фасад `useUserEnvironment`.
   Фасад стор не объединяет и не должен: три сигнала смотрят в три несвязанных
   источника, объединять нечего. Байт-идентичность копий `library/*` и
   `useUserEnvironment/internal/*` — это барьер форка, то есть цель, а не
   дублирование по недосмотру. Обе ветки одновременно в один проект не берут.

3. **React Compiler не включаем; готовность к нему держим.** Решение разработчика:
   ручная мемоизация остаётся — «перебдеть дешевле, чем ловить редкие
   расхождения». Компилятор — не часть React 19, а отдельный плагин сборки
   (`babel-plugin-react-compiler`); в проекте его нет, `@vitejs/plugin-react`
   стоит без него.

   Цена включения, если решение когда-нибудь пересмотрят: в сборку возвращается
   Babel (сейчас она 733 мс без него), бандл подрастает на генерируемый код
   кэширования, появляется класс труднодиагностируемых расхождений «компилятор
   решил иначе». Выигрыш: два ручных кэша идентичности (`slideCache`,
   `laneCacheRef`) и часть `useMemo`/`useCallback` становятся не нужны.

   **Готовность — требование к правкам, а не фон.** Правила `react-hooks/*`,
   включая правила React Compiler, включены во всём репозитории; ослаблять их
   или гасить файлами нельзя. Те же правила защищают от StrictMode и
   Offscreen-рендера, а не только от компилятора. Полки в `shared/**` обязаны
   работать и с ним, и без него: их копируют в проекты, где решение может быть
   другим.

4. **Git-хуков нет, и это решение, а не пробел.** Правило «коммит — отдельный
   вызов, никогда в одной команде с проверками» живёт текстом в `CLAUDE.md`
   обоих уровней и на полке правил. Предложенный `pre-commit`, который физически
   не пустил бы коммит на красном `typecheck`/`lint`, **отклонён**: жёсткое
   ограничение здесь не нужно.

   Цена решения названа честно: правило можно нарушить, и однажды это уже
   случилось — коммит прошёл на красном линте, потому что проверка и коммит
   стояли одной командой через `;`, да ещё и с трубой, которая уничтожает код
   возврата. Взамен: коммит на красном остаётся возможен **по прямой команде
   разработчика** («коммить как есть»), без обхода запрета и без `--no-verify`,
   и каждый такой случай стоит в отчёте прямым текстом. Плюс не платится ~20
   секунд на каждый коммит, из которых большинство — документные, и правило
   проекта прогона для них не требует вовсе.

   Вместо: если решение когда-нибудь пересмотрят, хук обязан уметь пропускать
   правки только в `*.md` и `.context/**` — иначе он станет тем, что обходят.

---

## I. Проход «вырезать историю» (выполнен)

По решению разработчика из кода, доков, README и шапок тестов вырезаны **следы
прошлой разработки**: замеры, модели устройств, «раньше было / мы починили /
мы пробовали», названия прошлых регрессий. **Оставлены** — и там, где нужно,
переписаны в явную форму `CONSTRAINT — …`:

- само ограничение;
- на что оно влияет;
- что сломается, если его убрать.

Затронуто ~35 файлов. Правки — только в комментариях, документации и
названиях `it(...)`; исполняемый код не менялся. Копии в
`kinetic/internal/**` и `environment/library/**` правились **парно** — сверка
диффом после прохода вернула прежние расхождения (2 / 9 / 9 / 9 / 6 / 6), то
есть форки не разъехались.

Попутно исправлена мохибейка в трёх местах (`≤`, `≈`, `±`) — в `src` её больше
нет.

Проход был неполон, и это нашлось при повторном чтении: в движке жестов и в его
форке оставались четыре ссылки «(the ride-crawl fix)» — имя прошлой регрессии в
комментариях `swipe/internals/math.ts` и `swipe/usePointerSwipe.ts`. Убраны парно,
ссылка на раздел README сохранена. Сейчас в `src` таких следов ноль.

**Как это читать дальше.** Ни одно утверждение о поведении на устройстве,
о производительности и о том, «что будет, если убрать», больше не подкреплено
чужим замером. Всё это — гипотезы, которые мы проверяем сами, когда доходим
до соответствующего участка.

---

## J. Как перепроверить всё это одной командой

Скрипт анализа графа лежит в репозитории — `.context/graph.mjs`. Запускать из
корня репозитория (`c:\dev\CarouselCC`):

```
node .context/graph.mjs brief X  # досье на адрес: связи, тесты, все записи базы
node .context/graph.mjs plan X   # что придётся тронуть: радиус, близнец, тесты, записи
node .context/graph.mjs blast X  # радиус адреса: прямые импортёры + транзитивно
node .context/graph.mjs cycles   # циклические импорты
node .context/graph.mjs blast    # радиус поражения + файлы без импортёров
node .context/graph.mjs dead     # экспорты, которые никто не импортирует
node .context/graph.mjs verify   # сверка базы с кодом, весь перечень из § J
node .context/graph.mjs twins    # правка форка: не забыт ли близнец; и правка
                                 # правил: не забыта ли полка правил
node .context/graph.mjs tested   # правка кода: тронуты ли её тесты
node .context/graph.mjs mutated  # правка кода: под мутациями ли она была
node .context/graph.mjs open     # гипотезы, дыры в тестах, узлы без якоря
node .context/graph.mjs sizes    # непустых строк на файл (в базе их нет)
```

Два ограничения инструмента, оба честные и оба видны в выводе:

1. Файл, из которого делают `export *`, из анализа мёртвых экспортов
   исключается — нельзя разобрать, что именно утянули.
2. Импорт **сразу после импорта-побочного-эффекта** не разбирается: в
   `main.tsx` строка `import "./globals.scss";` не имеет `from`, и разбор
   проглатывает следующую за ней строку целиком. Поэтому `dead` показывает
   `app/App.tsx → default`, хотя `main.tsx` его импортирует. Единственный такой
   случай в проекте; лечится перестановкой строк, но перестановка сместила бы
   порядок загрузки стилей, а это уже правка поведения.

`verify` сверяет всё перечисленное ниже и падает, если провалилась любая сверка.
**Числа проверок здесь нет намеренно:** разделов в выводе меньше, чем сверок —
часть разделов покрывает по две, — и любое одно число тут же расходится с одним
из трёх счётов, а с добавлением сверки устаревает молча. Перечень и есть
определение.

| Что сверяется | Как |
| --- | --- |
| покрытие карты | каждый файл кода назван в `00-map.md` |
| покрытие тестов | каждый тестовый файл назван в `08-tests.md` |
| пометки `CONSTRAINT` | каждая пометка в коде описана в `07-invariants.md` |
| пометки решений | каждая пометка в коде описана в `09-decisions.md` |
| правила направления | таблица слоёв из `03-graph.md` сверена с графом импортов |
| якоря `file:line` | файл существует, номер строки не выходит за его длину |
| цитаты у якорей | цитата найдена на тех самых строках, куда указывает якорь |
| объявленный состав | `(N файлов)` у папки совпадает с диском; объём строк не заявляется |
| радиус поражения | «N импортёров (+M тестовых)» совпадает с графом импортов |
| ссылки на документацию | каждый путь `*.md`, названный в комментарии кода, ведёт в существующий файл |
| копия инструмента | `graph.mjs` на полке правил совпадает с рабочим байт в байт |
| разрешения среды | `.claude/settings.json` проекта содержит **всё**, что перечисляет шаблон полки (`settings.template.json`); проектные добавки законны, потеря — нет |
| пути в обратных кавычках | адрес с косой чертой и расширением, названный в базе или документации, существует на диске; известные исключения перечислены в `CONFIG` с причиной |
| имена констант в тексте | составное имя из заглавных букв с подчёркиванием, названное внутри кода-спана, есть в исходниках: переименование константы иначе оставляет в тексте имя, которого нет |
| тесты лежат в `tests/` | ни один `*.test.ts(x)` не лежит вне папки `tests/` своего слоя — раскладку держит проверка, а не привычка |
| скиллы проекта | каждый скилл из `.claude/skills/**` объявлен в таблице «Скилл / Шаблон на полке», каждый объявленный существует, и его шаблон на полке совпадает с рабочим файлом байт в байт. Агенты не проверяются: это возможности окружения, а не файлы репозитория |
| разделы правил классифицированы | у каждого раздела `##` в `CLAUDE.md` есть строка в таблице «Раздел правил проекта / Где на полке»: либо адрес на полке, либо пометка «проектное». Заголовки с шаблоном напрямую **не** сверяются — он обобщённый, и у проекта законно есть свои разделы; ловится другое: раздел появился, а решение «едет на полку или нет» не принял никто |
| отложенное: закрытых нет, ссылки живые | ни один заголовок `02-todo.md` не помечен закрытым — сделанное удаляют, а не отмечают; и ни одна ссылка вида «пункт N» не ведёт в несуществующий пункт. Второе ловит то, что случается при каждом закрытии: пункт удаляют, следующие сдвигаются, а ссылка на прежний номер остаётся висеть сразу в нескольких файлах. Считается только там, где рядом назван сам `02-todo.md` или `deferred-work list`, иначе сверка ловила бы «пункт 3» любого другого перечня и врала бы |
| якорь не на скобке | якорь без цитаты не указывает на пустую строку или закрывающую скобку: туда попадает только съехавший |
| цитата у нового якоря | каждый якорь, **добавленный этой правкой**, несёт цитату; старые 185 не переписываются |
| названный файл существует | обратная сторона «покрытия карты»: запись про файл, которого нет на диске, читается как описание живого кода. Шаблоны со звёздочкой и перечисления расширений адресами не считаются, неоднозначное имя — не отсутствие |
| названный тест существует | обратная сторона «покрытия тестов»: строка реестра, называющая тест, которого нет на диске, — гарантия, которой больше не существует, и читается она как действующая. Неоднозначное имя (у парных форков тесты одноимённые) отсутствием не считается |
| нераспознанные пути | заявки состава папок, заявки импортёров и якоря `file:line` разрешаются в файл. Голые имена без номера строки этой сверкой **не** покрыты — их ловят «пути в обратных кавычках» (там, где есть папка) и строка выше (там, где это тест) |
| полка правил не отстаёт | таблица машинных сверок описывает одинаковое их число в `01-facts.md` и в `src/shared/context/knowledge-base.md`. Сверяются не формулировки — полка пишет обобщённо, без имён файлов проекта, — а счёт: завёл сверку, описал в одном месте и забыл про другое → красный прогон. Именно так полка отстала в первый раз, и молча |
| найденное исправлено | три сети сразу: в коде нет маркеров отложенной работы (`TODO`, `FIXME`, `HACK`, `XXX`, `ВРЕМЕННО`, `ПОТОМ`, `ПОЧИНИТЬ`); ни один абзац базы не паркует дефект книжным оборотом («вынесено разработчику», «в задачу не входило») без ссылки на `09-decisions.md`; и каждый абзац раздела, чей заголовок сам объявляет несделанную работу («Не сделано, и почему»), называет решение. Ловятся **записанные** формы откладывания; молчание — находку, которую просто не записали, — машина не видит, и это остаётся на ревью |

### Скиллы проекта

Скилл — не пассивная надстройка: перечень собирается **на старте сессии**, и
загружается скилл только по вызову — вашему (`/имя`) или моему, если описание в
его шапке подходит задаче. Отсюда два следствия, стоившие проверки: **новый
скилл в текущей сессии не появится** (нужен перезапуск или `/clear`), а
`user-invocable: true` в шапке — то, чем скилл делается доступным по `/`.
Раскладка на диске: `.claude/skills/<имя>/SKILL.md`.

Таблица ниже сверяется машиной трижды: объявленный существует, существующий
объявлен, и шаблон на полке совпадает с рабочим файлом байт в байт.

| Скилл | Шаблон на полке |
| --- | --- |
| task | task.skill.template.md |

**Агенты в эту таблицу не входят и проверены быть не могут:** это не файлы
репозитория, а возможности окружения. Те, на которые опираются правила, названы
прозой — `Explore` для широких сметаний, плюс команды `/code-review` и `/run`;
их состав и критерии — в `CLAUDE.md` и на полке в `environment.md`.

### Сверка в одну сторону — самый живучий класс дефекта здесь

Записан по итогу дня, где он дал больше половины находок. Сверка вида «каждый
файл на диске назван в базе» выглядит полной и таковой не является: обратная
сторона — «каждое имя в базе есть на диске» — молчала, и удалённый файл оставлял
запись, которая **читается как описание живого кода**. То же было у реестра
тестов, у списка разрешений среды и у состава скиллов; каждый раз выяснялось не
из рассуждения, а из пробы: удалить и посмотреть, покраснеет ли.

Отсюда правило для любой новой сверки: **спросить, что она говорит, когда
предмет исчез.** Если ответ «ничего» — сверка односторонняя, и вторая сторона
заводится сразу, а не «когда понадобится». И вторая ловушка при этом: инструмент
молчит одинаково и когда файла нет, и когда имя **неоднозначно** — у парных
форков одноимённых файлов по два. Неоднозначность отсутствием считать нельзя,
иначе список наполнится живыми файлами и его перестанут читать.

### Разделы правил проекта — что едет на полку, а что остаётся здесь

Таблица существует не для описания, а для **решения**: новый раздел в `CLAUDE.md`
без строки здесь роняет `verify`. Это единственный момент, когда вопрос «а на
полку это едет?» ещё дёшево задать — потом он не задаётся вовсе, и полка увозит
в следующий проект не все правила. Сверяется наличие решения, а не текст:
формулировки на полке обобщённые, и совпадение слов было бы ложной целью.

| Раздел правил проекта | Где на полке |
| --- | --- |
| База знаний — карта кода, а не замена чтению | `knowledge-base.md` |
| Это не приложение | проектное |
| Что из этого следует для оценки и для правок | проектное |
| Соразмерность: что читать под задачу | `workflow.md` |
| Проверка в настоящем движке | `workflow.md` |
| Как добавить новый узел | `workflow.md` |
| Как убрать узел | `workflow.md` |
| Планка качества — сверяется, а не подразумевается | `quality.md` |
| Коммиты — атомарно по смыслу, на риске дробнее | `workflow.md` |
| Форки обязаны оставаться синхронными по существу | `workflow.md` |
| Публичная поверхность — не ломать молча | `CLAUDE.template.md` |
| Стенд правим только по необходимости | проектное |
| Настройки и константы — не правка кода | `CLAUDE.template.md` |
| React Compiler выключен, готовность к нему обязательна | проектное |
| Тесты правятся вместе с кодом, а не после него | `workflow.md` |
| Мутационный прогон — машинная форма F1, а не отдельная кампания | `workflow.md` |
| Поиск и чтение файлов — так, чтобы не всплывали подтверждения | `environment.md` |
| Как проверяется сделанное | `workflow.md` |
| Отчёт: найденное и сделанное называются прямо | `workflow.md` |
| Граф считается, а не додумывается | `CLAUDE.template.md` |
| Документация следует за кодом, а не наоборот | `knowledge-base.md` |
| База знаний обновляется вместе с кодом | `knowledge-base.md` |
| Куда писать отчёты и черновики | `workflow.md` |
| Крупная задача: сначала план | `workflow.md` |

Три послабления, все намеренные. Файл засчитывается упомянутым по имени с
расширением **или** без него — база пишет и полными именами, и группами вида
`{a,b}.ts` (группы раскрываются). Ambient-объявления (`*.d.ts`) из покрытия
исключены: в них нет ни поведения, ни связей. Состав папки считается по коду без
тестов, а если в пути есть `tests` — наоборот, только по тестам.

Чего инструмент **не** видит: число, записанное прозой или в форме, которой нет
в README базы. Такое число молча остаётся непроверенным — поэтому размеры пишут
только тремя объявленными формами.

---

## K. Ловушки окружения (собраны на практике)

Не про код проекта, а про инструменты, которыми его правят. Каждая уже стоила
времени.

1. **Все файлы в рабочем дереве — CRLF** (`core.autocrlf=true`, в индексе LF;
   с версии `.gitattributes` политика зафиксирована как `eol=lf`). Отсюда:
   любая замена многострочного фрагмента, которая ищет по одному лишь символу
   перевода строки (LF), **не найдёт ничего**. Читать файл, нормализовать концы
   строк, править, писать обратно в том же виде. Однострочные замены проходят
   и без этого — и тем опаснее, потому что создают ложное ощущение, что метод
   работает.

2. **`node -e` в этом окружении теряет один уровень экранирования.** Строка
   «обратный слэш плюс s» доезжает до JS уже без слэша, то есть как обычная
   буква — поиск по такой строке молча не находит ничего, и это выглядит как
   «в файле нет искомого». Лечится так: скрипт пишется в файл (heredoc с
   закавыченным разделителем) и запускается как `node file.mjs`, а в поисковых
   строках экранирования лучше не иметь вовсе — искать по фрагменту без него.
   Тот же эффект бьёт и по heredoc, поэтому проверять надо результат, а не
   намерение.

3. **`grep` с диапазоном `[а-яА-Я]` в этой локали срабатывает на тире и
   эмодзи** — сравнение идёт побайтно. Проверка «есть ли в коде кириллица» так
   даёт сотни ложных срабатываний. Считать по кодовым точкам
   (`0x0400–0x04FF`), а метод сверять на файле, про который заранее известно,
   есть там кириллица или нет.

4. **Prettier ломает многострочный комментарий внутри JSX-тернарника**: склеивает
   строки в одну и переставляет куски местами, а результат не идемпотентен —
   `--check` остаётся красным после `--write`. Единственное известное место —
   `SlideItem.tsx`, где объяснение теперь вынесено НАД выражением. Если такой
   комментарий появится снова, чинить так же, а не подгонять форматтер.

5. **PowerShell-синтаксис here-string (`@'…'@`) в Bash не работает** — попадает
   в текст как есть. Сообщения коммитов писать во временный файл и передавать
   через `git commit -F`.
