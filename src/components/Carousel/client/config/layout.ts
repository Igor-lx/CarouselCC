// Render-window buffer. See docs/config/layout.md for what it governs.

/** Render-window buffer in page screens (pre-mounts every reachable slide). */
export const RENDER_WINDOW_BUFFER_MULTIPLIER = 2;
