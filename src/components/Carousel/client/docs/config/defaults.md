# config/defaults.ts — public-prop defaults

`CAROUSEL_DEFAULTS` holds the value substituted for each public prop when the
caller leaves it `undefined`. Each key backs the same-named prop, documented in
[../architecture/public-api.md](../architecture/public-api.md). The values are
the out-of-the-box demo settings, not part of the behavioral contract.
