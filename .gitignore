node_modules/
dist/
coverage/
reports/
.stryker-tmp/
# Артефакты смоука в браузере: трассы и отчёт пишутся на каждый прогон.
test-results/
playwright-report/
.DS_Store
*.local

# Design-time image masters — kept locally / backed up elsewhere, not in the repo.
/assets/
